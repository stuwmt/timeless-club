spring:
  application:
    name: timeless-club-auth-dev
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: xxxxxxxx
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: xxxxxxxx